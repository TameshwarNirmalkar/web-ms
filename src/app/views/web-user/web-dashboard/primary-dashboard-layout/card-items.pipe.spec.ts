import { CardItemsPipe } from './card-items.pipe';

describe('CardItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CardItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
